<body>
  <div class="container">
    <div class="col-xs-8 affix nav-stacked" id="right">
      <img class = "logo logo_default " src="/media/tortoise.jpg" height="1400" width="1400">
    </div>
  </div>

  <div class="row">
    <div class="col-xs-4" id="left-column">
      <%= link_to 'Update', full_update_path, class: 'btn btn-warning'%>
      <table class="table table-bordered table-striped">
        <tr>
          <th>st number</th>
          <th>st name</th>
          <th># of racks</th>
        </tr>

        <% @bike_racks.each do |bike_rack| %>
          <tr>
            <td><%= bike_rack.street_number %></td>
            <td><%= bike_rack.street_name %></td>
              <td><%= bike_rack.number_of_racks %></td>
          </tr>
        <% end %>
      </table>
    </div>
  </div>
<body>